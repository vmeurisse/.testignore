# If the pattern does not contain a slash /, Git treats it as a shell glob pattern and checks for a
# match against the pathname relative to the location of the .gitignore file (relative to the
# toplevel of the work tree if not from a .gitignore file).

noSlash

folder1

folder4

file1

file7*
file8**
